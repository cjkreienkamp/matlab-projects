function [a, b] = swap(x, y)

% Chris Kreienkamp
% Section 10
% 2/21/17
%
% [a, b] are the output variables. There are two outputs.
% SWAP is the name of the function and file.
% (x, y) are the input variables. There are two inputs.
%
% The SWAP function takes the values x and y as inputs
% and changes their position in the vector such that
% a = y and b = x

a = y;
b = x;